APPSERVER_TYPE="${APPSERVER_TYPE:-wildfly}"
#APPSERVER_TYPE="${APPSERVER_TYPE:-jboss-eap}""
APPSERVER_VERSION="${APPSERVER_VERSION:-13.0.0.Final}"
#APPSERVER_VERSION="${APPSERVER_VERSION:-7.1.0}"
APPSERVER_DOWNLOAD_URL="${APPSERVER_DOWNLOAD_URL:-http://download.jboss.org/$APPSERVER_TYPE/$APPSERVER_VERSION/$APPSERVER_TYPE-$APPSERVER_VERSION.zip}"

LIFERAY_PREFIX="${LIFERAY_PREFIX:-liferay-ce-portal}"
#LIFERAY_PREFIX="${LIFERAY_PREFIX:-liferay-dxp-digital-enterprise}"
LIFERAY_FULL_VERSION="${LIFERAY_FULL_VERSION:-7.1.0-ga1-20180703012531655}"
#LIFERAY_FULL_VERSION="${LIFERAY_FULL_VERSION:-7.0-sp3-20170503123037723}"
LIFERAY_BASE_URL="${LIFERAY_BASE_URL:-https://sourceforge.net/projects/lportal/files/Liferay%20Portal/7.1.0%20GA1}"

LIFERAY_HOME="${LIFERAY_HOME:-$TEMP_PATH/liferay}"
APPSERVER_HOME_PATH="${APPSERVER_HOME_PATH:-$LIFERAY_HOME/$APPSERVER_TYPE}"

LIFERAY_COPY_CLUSTER_PATH="${LIFERAY_COPY_CLUSTER_PATH:-$TEMP_PATH/cluster}"
LIFERAY_COPY_DEPLOYMENT_PATH="${LIFERAY_COPY_DEPLOYMENT_PATH:-$TEMP_PATH/deploy}"

JDBC_DRIVER_DOWNLOAD_URL="${JDBC_DRIVER_DOWNLOAD_URL:-https://jdbc.postgresql.org/download/postgresql-42.2.4.jar}"
JDBC_DRIVER_CLASSNAME="${JDBC_DRIVER_CLASSNAME:-org.postgresql.Driver}"
JDBC_DRIVER_CONNECTION_URL="${JDBC_DRIVER_CONNECTION_URL:-jdbc:postgresql:ddd}"
JDBC_DRIVER_USERNAME="${JDBC_DRIVER_USERNAME:-postgres}"
JDBC_DRIVER_PASSWORD="${JDBC_DRIVER_PASSWORD:-postgres}"
JDBC_CREDENTIAL_STORE_PASSWORD="${JDBC_CREDENTIAL_STORE_PASSWORD:-cs_pass}"

MAIL_CREDENTIAL_STORE_PASSWORD="${MAIL_CREDENTIAL_STORE_PASSWORD:-cs_pass}"
MAIL_HOST="${MAIL_HOST:-smtp.yandex.ru}"
MAIL_PORT="${MAIL_PORT:-465}"
MAIL_USERNAME="${MAIL_USERNAME:-yandex@yandex.ru}"
MAIL_PASSWORD="${MAIL_PASSWORD:-mail_pass}"
MAIL_ENABLE_SSL="${MAIL_ENABLE_SSL:-true}"
MAIL_ENABLE_TLS="${MAIL_ENABLE_TLS:-false}"

PROMETHEUS_DOWNLOAD_URL="${PROMETHEUS_DOWNLOAD_URL:-http://central.maven.org/maven2/io/prometheus/jmx/jmx_prometheus_javaagent/0.3.1/jmx_prometheus_javaagent-0.3.1.jar}"
PROMETHEUS_COPY_CONFIG_PATH="${PROMETHEUS_COPY_CONFIG_PATH:-$TEMP_PATH/prometheus/config.yaml}"

JKS_FILE_PATH="${JKS_FILE_PATH:-$SCRIPT_DIR/server.keystore}"
JKS_STORE_PASSWORD="${JKS_STORE_PASSWORD:-change_store_pass}"
JKS_KEY_PASSWORD="${JKS_KEY_PASSWORD:-change_key_pass}"

ELASTIC_TRANSPORT_ADDRESSES="${ELASTIC_TRANSPORT_ADDRESSES:-127.0.0.1:9300}"
ELASTIC_CLUSTER_NAME="${ELASTIC_CLUSTER_NAME:-clustery_mcclusterface}"
